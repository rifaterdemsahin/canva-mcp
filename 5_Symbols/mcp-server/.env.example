# Canva API Credentials
# 1. Go to https://www.canva.com/developers/
# 2. Create or select your app
# 3. Copy the Client ID and Client Secret
#
# Real values live in Azure Key Vault "dp-kv-deliverypilot" (do NOT create a new vault):
#   ../toolbox/secrets.sh get canva-mcp-CANVA-CLIENT-ID
#   ../toolbox/secrets.sh get canva-mcp-CANVA-CLIENT-SECRET

CANVA_CLIENT_ID=your_canva_client_id_here
CANVA_CLIENT_SECRET=your_canva_client_secret_here

# User access token from the OAuth PKCE flow (auth.html) — required by
# `npm run canva:create`, `npm run canva:whoami`, and the create_design /
# upload_assets MCP tools. Expires after ~4h; re-run auth.html to refresh.
CANVA_ACCESS_TOKEN=your_canva_access_token_here

# Optional: debug logging
LOG_LEVEL=info

# Copy this file to .env (gitignored) and fill in real values:
#   cp .env.example .env
# Client ID/secret come from the vault:
#   ../toolbox/secrets.sh get canva-mcp-CANVA-CLIENT-ID
#   ../toolbox/secrets.sh get canva-mcp-CANVA-CLIENT-SECRET
